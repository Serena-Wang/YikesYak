<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demouser.yikesyak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
Home_Layout
        <activity android:name=".HomePage">
master
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:label= "@string/app_name" android:name=".ComplimentsSection"></activity>
        <activity android:label= "@string/app_name" android:name=".ConfessionSection"></activity>
        <activity android:label= "@string/app_name" android:name=".QASection"></activity>

Home_Layout
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
 master
    </application>

</manifest>